
    <view class="_4039f44">
        
    <swiper class="swiper _4039f44" indicator-dots="{{$vSwper$swiperConfig.indicatorDots}}" autoplay="{{$vSwper$swiperConfig.autoplay}}" interval="{{$vSwper$swiperConfig.interval}}" duration="{{$vSwper$swiperConfig.duration}}">
        <block  wx:for="{{$vSwper$alldata}}" wx:key="id">
            <swiper-item>
                <image src="{{item.imgUrl}}" style="width: 100%; height=100%;" mode="aspectFill" data-id="{{item.id}}" bindtap="$vSwper$tap" lazy-load="true"/>
            </swiper-item>
        </block>
    </swiper>

        <view class="_4039f44">
            
    <view class="title _c90e8d0 _4039f44" data-leftText="{{$vBaseTitle$leftText}}" data-rightText="{{$vBaseTitle$rightText}}" bindtap="$vBaseTitle$tap">
        <text class="left _c90e8d0">{{$vBaseTitle$leftText}}</text>
        <view class="right _c90e8d0">
            <text class="_c90e8d0">{{$vBaseTitle$rightText}}</text>
        </view>
    </view>
            <block class="_4039f44" wx:for="{{hotTopic}}" wx:for-index="index" wx:for-item="item" wx:key="index">
                
    <view class="text _2d225a8 _4039f44" bindtap="$vText$tap" data-text="{{item.name}}" data-com-index="{{index}}">{{item.name}}</view>
            </block>
        </view>
        
    <view class="title _ec4efbe _4039f44">{{$vTitle$title}}</view>
        
    <view class="list _199b741 _4039f44">
        <block class="_199b741" wx:for="{{$vGrid$list}}" wx:for-index="$vGrid$index" wx:for-item="$vGrid$item" wx:key="$vGrid$index">
            <image class="img _199b741" style="{{$vGrid$imgStyle}}" src="{{$vGrid$item.url}}" mode="aspectFill" data-id="{{$vGrid$item.id}}" bindtap="$vGrid$tap"/>
        </block>
    </view>
    </view>