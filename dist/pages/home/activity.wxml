
    <view class="activities _2294554">
        <block class="_2294554" wx:for="{{activities}}" wx:for-index="index" wx:for-item="item" wx:key="index">
            
    <view class="activity _be153ec _2294554" data-id="{{item.id}}" bindtap="$vActivity$tap" data-com-index="{{index}}">
        <image class="cover _be153ec" src="{{item.cover}}" mode="aspectFill"/>
        <view class="content-view _be153ec">
            <text class="title ellipsis _be153ec">{{item.title}}</text>
            <text class="description _be153ec">{{item.description}}</text>
            <!-- <text class="time">{{item.time}}</text>
            <text class="address">{{item.address}}</text> -->
            <!--
            <view class="last">
                <text class="money">{{item.money > 0 ? item.money : '免费'}}</text>
                <text class="join" type="default">报名</text>
            </view>
            -->
        </view>
    </view>
        </block>
    </view>